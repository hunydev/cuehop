# CueHop 코어 게임 규칙서

- 버전: MVP 1.0
- 장르: 블록 조립 논리 퍼즐
- 플랫폼: 반응형 웹
- 게임명: CueHop
- 태그라인: *Set the cue. Watch the frog hop.*

## 1. 게임 목표

플레이어는 주어진 규칙 블록을 조립해 개구리의 이동 프로그램을 만든다.

실행을 시작하면 엔진 변수 `E`가 한 틱마다 1씩 증가하고, 프로그램은 위에서 아래로 반복 실행된다. 플레이어는 제한된 에너지가 소진되기 전에 개구리를 목표 타일에 정확히 착지시켜야 한다.

개구리는 플레이어가 직접 조작하지 않는다. 플레이어는 규칙을 설계하고, 개구리는 그 규칙을 따른다.

## 2. 좌표 체계

- 모든 스테이지의 시작 좌표는 `(0, 0)`이다.
- `x`가 증가하면 오른쪽으로 이동한다.
- `y`가 증가하면 위쪽으로 이동한다.
- 좌표는 정수만 사용한다.
- 타일 밖의 좌표로 이동하면 절벽에서 추락한다.
- MVP에서는 음수 좌표도 타일 밖으로 판정한다.
- 맵의 좌측 아래 타일이 `(0, 0)`이다.

## 3. 엔진 변수 E

`E`는 게임의 시간과 박자를 나타내는 읽기 전용 엔진 변수다.

- 스테이지 시작 전 `E = 0`이다.
- `E = 0`인 실행 틱은 존재하지 않는다.
- 첫 번째 틱이 시작되면 `E = 1`이 된다.
- 이후 틱마다 반드시 1씩 증가한다.
- 플레이어는 `E`를 읽을 수 있지만 변경할 수 없다.
- 재시작하면 `E`는 다시 0이 된다.

화면에는 `엔진 박자 · E = 3`처럼 표시한다.

## 4. 한 틱의 실행 순서

한 틱은 다음 순서로 처리한다.

1. 에너지 1개를 소비한다.
2. `E`가 1 증가한다.
3. 프로그램의 첫 번째 블록부터 실행한다.
4. 블록을 위에서 아래로 순차 실행한다.
5. 조건 블록을 만나면 현재 값을 기준으로 조건을 판단한다.
6. 선택된 조건 내부의 블록을 위에서 아래로 실행한다.
7. `x` 또는 `y` 값이 바뀔 때마다 개구리가 즉시 점프한다.
8. 각 점프가 끝날 때마다 성공·추락·위험물·장벽 충돌을 판정한다.
9. 프로그램의 마지막 블록까지 실행하면 해당 틱이 끝난다.
10. 성공하거나 실패하지 않았다면 다음 틱을 시작한다.

목표에 도착하거나 실패 조건이 발생하면 남은 블록은 실행하지 않는다.

## 5. 이동 규칙

### 5.1 대입과 점프

`x를 4로` 블록이 실행되면 현재 x좌표와 관계없이 개구리의 x좌표가 4가 된다. 현재 위치가 `(1, 2)`라면 `(4, 2)`로 한 번에 점프한다.

`y를 3으로`가 실행되면 현재 x좌표는 유지되고 y좌표만 변경된다.

### 5.2 한 번의 대입은 한 번의 점프

- 좌표가 여러 칸 변해도 점프는 한 번이다.
- 이동 중 지나치는 일반 타일과 착지 위험물은 밟지 않는다.
- 착지 지점만 타일 위험 판정을 한다.
- 통과 불가능한 장벽은 점프로 넘어갈 수 없다.
- 대각선 점프는 존재하지 않는다.
- 한 블록에서는 x 또는 y 중 하나만 변경할 수 있다.

### 5.3 값이 변하지 않는 경우

현재 `x = 3`일 때 `x를 3으로`가 실행되면:

- 점프하지 않는다.
- 오류로 처리하지 않는다.
- 개구리가 잠시 기다리는 애니메이션을 재생한다.
- 다음 블록은 정상적으로 실행한다.
- 해당 틱의 에너지는 그대로 소비된다.

기다림은 정식 퍼즐 행동이다.

### 5.4 같은 축을 여러 번 변경하는 경우

다음 프로그램은 한 틱 안에 세 번 점프한다.

```text
x를 1로
x를 2로
x를 4로
```

각 블록은 독립적으로 실행되며, 각 착지 후 성공과 실패를 판정한다.

## 6. 목표 판정

- 개구리가 목표 타일에 정확히 착지하면 즉시 성공한다.
- 점프 도중 목표 타일 위를 지나가는 것은 성공이 아니다.
- 성공하면 해당 틱과 남은 프로그램 실행을 즉시 종료한다.
- 시작 타일과 목표 타일은 같은 위치에 배치하지 않는다.

## 7. 타일과 장애물

### 7.1 안전 타일

착지할 수 있는 일반 타일이다.

### 7.2 목표 타일

정확히 착지하면 스테이지에 성공한다.

### 7.3 착지 위험물

중간을 뛰어넘을 수 있지만 착지하면 실패한다.

예: 구멍, 물, 가시, 독 타일, 깨지는 타일

### 7.4 절벽

맵 밖으로 착지하면 개구리가 아래로 추락하고 실패한다.

### 7.5 통과 장벽

타일과 타일 사이에 배치한다.

예: 돌벽, 덩굴 장벽, 마법 장벽, 레이저 울타리

점프 경로가 장벽을 통과하면 개구리가 장벽에 부딪히고 실패한다. 착지 지점이 안전해도 장벽은 통과할 수 없다.

통과 장벽은 첫 5개 튜토리얼 이후에 도입한다.

## 8. 에너지와 실행 종료

각 스테이지에는 사용할 수 있는 최대 에너지가 있다.

- 한 틱을 시작할 때 에너지 1개를 소비한다.
- 한 틱 안에서 여러 번 점프해도 추가 에너지는 소비하지 않는다.
- 마지막 에너지로 시작한 틱은 프로그램 끝까지 실행할 수 있다.
- 마지막 틱이 끝날 때까지 목표에 도착하지 못하면 실패한다.
- 움직이지 않는 틱도 에너지를 소비한다.

실행 종료 조건:

- 목표 타일 착지: 성공
- 맵 밖으로 이동: 실패
- 위험물 착지: 실패
- 통과 장벽 충돌: 실패
- 에너지 소진: 실패
- 플레이어가 실행 중지: 실패 기록 없이 편집 상태로 복귀

## 9. MVP 블록 문법

### 9.1 이동 블록

- `x를 [값]으로`
- `y를 [값]으로`

### 9.2 값 블록

- `E`
- 정수 상수

MVP에서는 `x`와 `y`를 식의 입력값으로 읽지 않는다. `x`와 `y`는 이동 결과를 지정하는 대상으로만 사용한다.

### 9.3 계산 블록

- 더하기 `+`
- 빼기 `−`
- 곱하기 `×`

나눗셈, 나머지, 함수, 사용자 변수는 MVP에서 제외한다.

계산 블록은 중첩할 수 있다.

```text
E × 2
E + 1
(E × 2) − 1
```

### 9.4 조건 블록

화면에서는 프로그래밍 문법 대신 자연어로 표현한다.

```text
E가 2일 때
    x를 E + 1로
그 밖에는
    x를 E로
```

지원 비교:

- 같다
- 같지 않다
- 크다
- 작다
- 크거나 같다
- 작거나 같다

MVP 조건문의 왼쪽 값은 `E`, 오른쪽 값은 정수 또는 계산식으로 제한한다.

### 9.5 조건 중첩

- 조건 블록 내부에 다른 조건 블록을 넣을 수 있다.
- 모바일 가독성을 위해 최대 중첩 깊이는 2단계다.
- 중괄호는 사용하지 않는다.
- 포함 관계는 들여쓰기, 배경색, 연결선으로 표현한다.
- `그 밖에는` 영역은 선택 사항이다.

## 10. 블록 조립 규칙

- 스테이지마다 사용할 수 있는 블록 종류가 정해진다.
- 튜토리얼에서는 필요한 블록을 정해진 수량만 제공한다.
- 일반 스테이지에서는 블록 종류와 최대 코드 줄 수를 제한한다.
- 불완전한 블록이 있으면 실행할 수 없다.
- 실행 전 오류가 있는 슬롯을 강조한다.
- 블록은 탭 또는 드래그로 추가할 수 있다.
- 위·아래 이동 버튼으로 순서를 변경할 수 있다.
- 블록을 복제하거나 삭제할 수 있다.
- 조건 블록 안으로 다른 블록을 끌어 넣을 수 있다.

코드 줄 수 계산:

- 이동 블록 하나: 1줄
- 조건 블록 하나: 1줄
- 조건 내부 이동 블록: 각각 1줄
- `그 밖에는` 영역 자체는 줄 수에 포함하지 않는다.
- 값과 계산 블록은 별도의 코드 줄로 계산하지 않는다.

## 11. 실행 화면 기능

필수 기능:

- 실행
- 일시정지
- 다음 틱 실행
- 처음부터 재실행
- 실행 중지 후 편집
- 실행 속도 `×1`, `×2`
- 현재 `E` 표시
- 현재 좌표 표시
- 남은 에너지 표시
- 현재 실행 중인 블록 강조
- 선택된 조건 분기 강조

`다음 틱 실행`은 블록 하나가 아니라 프로그램 전체를 한 틱 실행한다.

프로그램을 재실행하면:

- 개구리가 `(0, 0)`으로 돌아간다.
- `E`가 0이 된다.
- 에너지가 초기화된다.
- 타일 상태가 초기화된다.
- 작성한 블록은 유지된다.

## 12. 성공과 실패 피드백

### 성공

- 목표 타일이 밝게 빛난다.
- 개구리가 목표에 착지하며 튀어 오른다.
- 남은 에너지와 사용한 블록 수를 표시한다.
- 별 획득 조건을 순서대로 공개한다.

### 절벽 추락

- 개구리가 점프 궤도를 벗어나 아래로 떨어진다.
- 실패를 일으킨 블록을 붉게 강조한다.
- 맵에는 마지막 안전 위치를 표시한다.

### 위험물 착지

- 위험물에 맞는 짧은 실패 애니메이션을 재생한다.
- 해당 착지 좌표와 실행 블록을 강조한다.

### 장벽 충돌

- 개구리가 벽에 부딪힌 후 되튕긴다.
- 통과할 수 없는 경로를 짧게 표시한다.

### 에너지 소진

- 개구리가 지쳐 주저앉는다.
- 마지막 위치와 다음에 실행될 예정이었던 `E` 값을 표시한다.

실패 후 즉시 편집 화면으로 전환하지 않는다. 플레이어가 실패 원인을 확인한 다음 편집 버튼을 누르게 한다.

## 13. 별점 구조

각 스테이지에는 별이 3개 있다.

- 별 1: 목표 타일 도착
- 별 2: 목표 틱 이내 도착
- 별 3: 스테이지별 특별 조건 달성

특별 조건 예시:

- 목표 코드 줄 수 이하
- 특정 수 이하의 점프
- 모든 수집물 획득
- 지정 블록 사용
- 에너지 일정량 이상 보존
- 특정 타일에 착지
- 정확한 횟수만큼 기다리기

별은 한 번에 전부 얻지 않아도 된다. 획득한 별은 브라우저 로컬 저장소에 기록한다.

---

# 튜토리얼 스테이지 1~5

## 튜토리얼 1 — 첫 번째 신호

- 영문명: First Cue
- 학습 목표: x축, `E`, 이동 블록, 실행 버튼
- 맵: 5×1
- 시작: `(0, 0)`
- 목표: `(4, 0)`
- 모든 타일 안전
- 에너지: 6

### 제공 블록

- `x를 [값]으로` 1개
- `E` 1개

### 정답

```text
x를 E로
```

### 실행 결과

| 틱 | E | 위치 |
|---:|---:|---|
| 시작 | 0 | `(0, 0)` |
| 1 | 1 | `(1, 0)` |
| 2 | 2 | `(2, 0)` |
| 3 | 3 | `(3, 0)` |
| 4 | 4 | `(4, 0)` 성공 |

### 별 조건

- 별 1: 골 도착
- 별 2: 4틱 이내 도착
- 별 3: 코드 1줄로 도착

### 안내 문구

> E는 한 박자마다 1씩 커져요.

> x를 E로 맞추면 개구리가 오른쪽으로 한 칸씩 점프해요.

## 튜토리얼 2 — 위를 향해

- 영문명: Look Up
- 학습 목표: y축, x축과 y축의 차이
- 맵: 1×5
- 시작: `(0, 0)`
- 목표: `(0, 4)`
- 모든 타일 안전
- 에너지: 6

### 제공 블록

- `y를 [값]으로` 1개
- `E` 1개

### 정답

```text
y를 E로
```

### 실행 결과

| 틱 | E | 위치 |
|---:|---:|---|
| 시작 | 0 | `(0, 0)` |
| 1 | 1 | `(0, 1)` |
| 2 | 2 | `(0, 2)` |
| 3 | 3 | `(0, 3)` |
| 4 | 4 | `(0, 4)` 성공 |

### 별 조건

- 별 1: 골 도착
- 별 2: 4틱 이내 도착
- 별 3: 코드 1줄로 도착

### 안내 문구

> y가 커지면 위쪽으로 점프해요.

## 튜토리얼 3 — 순서가 만드는 길

- 영문명: Order Makes the Path
- 학습 목표: 순차 실행, 한 틱 안의 복수 이동, 실행 순서
- 맵: 5×5
- 시작: `(0, 0)`
- 목표: `(4, 4)`
- 모든 타일 안전
- 에너지: 6

### 제공 블록

- `x를 E로` 1개
- `y를 E로` 1개

### 대표 정답 A

```text
x를 E로
y를 E로
```

### 대표 정답 B

```text
y를 E로
x를 E로
```

두 정답 모두 성공하지만 이동 경로의 꺾이는 방향이 다르다.

### 정답 A 실행 결과

| 틱 | E | 첫 번째 점프 | 두 번째 점프 |
|---:|---:|---|---|
| 1 | 1 | `(1, 0)` | `(1, 1)` |
| 2 | 2 | `(2, 1)` | `(2, 2)` |
| 3 | 3 | `(3, 2)` | `(3, 3)` |
| 4 | 4 | `(4, 3)` | `(4, 4)` 성공 |

### 별 조건

- 별 1: 골 도착
- 별 2: 4틱 이내 도착
- 별 3: 코드 2줄로 도착

### 안내 문구

> 한 박자 안에서도 블록은 위에서 아래로 차례대로 실행돼요.

> 블록의 순서를 바꾸면 점프 경로도 달라져요.

## 튜토리얼 4 — 멀리 뛰기

- 영문명: Leap Ahead
- 학습 목표: 계산 블록, 여러 칸 점프, 착지 판정, 에너지 제한
- 맵: 5×1
- 시작: `(0, 0)`
- 목표: `(4, 0)`
- `(1, 0)`: 구멍
- `(2, 0)`: 안전 타일
- `(3, 0)`: 구멍
- 에너지: 4

### 제공 블록

- `x를 [값]으로` 1개
- `E` 1개
- `×` 1개
- 숫자 `2` 1개

### 정답

```text
x를 E × 2로
```

### 실행 결과

| 틱 | E | 계산 결과 | 위치 |
|---:|---:|---:|---|
| 시작 | 0 | — | `(0, 0)` |
| 1 | 1 | 2 | `(2, 0)` |
| 2 | 2 | 4 | `(4, 0)` 성공 |

개구리는 `(1, 0)`과 `(3, 0)`의 구멍 위를 지나지만 착지하지 않으므로 안전하다.

### 별 조건

- 별 1: 골 도착
- 별 2: 2틱 이내 도착
- 별 3: 코드 1줄로 도착

### 안내 문구

> 좌표가 멀리 바뀌면 한 번에 멀리 점프해요.

> 위험한 타일도 착지만 하지 않으면 뛰어넘을 수 있어요.

## 튜토리얼 5 — 그때만 점프

- 영문명: Only Then
- 학습 목표: 조건 블록, `그 밖에는`, 위험 타일 건너뛰기, 기다리기
- 맵: 5×1
- 시작: `(0, 0)`
- 목표: `(4, 0)`
- `(2, 0)`: 가시 타일
- 나머지 타일 안전
- 에너지: 6

### 제공 블록

- `E가 2일 때` 조건 블록 1개
- `그 밖에는` 영역
- `x를 E + 1로` 1개
- `x를 E로` 1개

### 정답

```text
E가 2일 때
    x를 E + 1로
그 밖에는
    x를 E로
```

### 실행 결과

| 틱 | E | 선택된 분기 | x 결과 | 행동 |
|---:|---:|---|---:|---|
| 1 | 1 | 그 밖에는 | 1 | `(1, 0)`으로 점프 |
| 2 | 2 | E가 2일 때 | 3 | 가시를 넘어 `(3, 0)`으로 점프 |
| 3 | 3 | 그 밖에는 | 3 | 값이 같아 제자리에서 기다림 |
| 4 | 4 | 그 밖에는 | 4 | `(4, 0)`에 착지하여 성공 |

### 별 조건

- 별 1: 골 도착
- 별 2: 4틱 이내 도착
- 별 3: 코드 3줄 이하로 도착

### 안내 문구

> 조건이 맞을 때만 안쪽 블록을 실행해요.

> 좌표가 그대로라면 점프하지 않고 한 박자 기다려요.

---

# 튜토리얼 완료 기준

5개 튜토리얼을 완료한 플레이어는 다음을 이해해야 한다.

1. `E`는 매 틱 1씩 증가한다.
2. x는 가로, y는 세로 위치다.
3. 블록은 위에서 아래로 실행된다.
4. 한 틱에 여러 번 점프할 수 있다.
5. 계산으로 점프 거리를 바꿀 수 있다.
6. 중간 타일은 뛰어넘을 수 있다.
7. 조건에 따라 다른 행동을 실행할 수 있다.
8. 값이 변하지 않으면 기다린다.
9. 에너지가 소진되기 전에 목표에 착지해야 한다.
10. 목표 위를 지나가는 것만으로는 성공하지 않는다.

# MVP 외 확장 항목

다음 요소는 코어 구현에서 제외하고 이후 검토한다.

- `x`, `y`를 식의 입력값으로 읽기
- 나눗셈과 나머지
- 사용자 변수
- 함수
- 다중 개구리
- 스위치와 문
- 움직이는 장애물
- 수집물
- 스테이지 제작 및 공유
- AI 힌트와 플레이 분석
